# AI Usage Log

| Stage | Tool / Model | Concrete prompt used | Result | Human verification |
|---|---|---|---|---|
| Requirement analysis | OpenAI Codex / GPT-5 | "Read the P6 assignment text. Extract only Assignment 2 requirements for an AI-assisted version of the telecom billing MVP. Do not add extra features." | Scope limited to equivalent MVP, AI usage log, OWASP analysis, comparison table and report. | Checked against the PDF text. |
| Architecture | OpenAI Codex / GPT-5 | "Design a small FastAPI telecom billing MVP for Assignment 2 with UI, backend, PostgreSQL, Redis, JWT auth, two roles, three entities and 200+ seed rows. Keep it separate from the manual version." | Selected FastAPI, SQLAlchemy, PostgreSQL, Redis, static frontend and Docker Compose. | Compared with assignment minimum requirements. |
| Data model | OpenAI Codex / GPT-5 | "Create an independent data model for customers, plans and invoices. It must support client/admin roles, active plan selection, invoice ownership and duplicate invoice prevention by customer and month." | Created `Customer`, `Plan`, `Invoice`, role enum, invoice status enum and unique constraint. | Checked that it maps to the required business scenario. |
| Authentication | OpenAI Codex / GPT-5 | "Implement JWT login with bcrypt password hashing, strong password validation and Redis failed-login limiting. Avoid logging passwords, tokens, email or request bodies." | Added password hashing, JWT issuing/decoding, Pydantic validation and Redis rate limit helpers. | Reviewed log statements and schema fields. |
| Authorization | OpenAI Codex / GPT-5 | "Add RBAC and object-level authorization: clients can see only their own invoices, admins can generate invoices, and invoice generation must require a separate internal billing key." | Added `current_customer`, `require_admin`, `X-Internal-Billing-Key` and 404 response for foreign invoices. | Checked endpoint behavior against access matrix. |
| Billing logic | OpenAI Codex / GPT-5 | "Implement invoice generation so the request accepts only customer_id and billing_month. The backend must calculate amount from the active plan and reject duplicates." | Generated backend-calculated invoices and duplicate detection. | Checked against insecure design and data integrity risks. |
| Frontend | OpenAI Codex / GPT-5 | "Build a minimal static frontend for the AI-assisted billing MVP with pages for dashboard, plans, invoices and admin billing. Use separate HTML, CSS and JS files." | Added `app/web/index.html`, `styles.css`, `app.js`. | Checked static links and API endpoint names. |
| Docker | OpenAI Codex / GPT-5 | "Create Dockerfile, docker-compose.yml and Makefile for the FastAPI/PostgreSQL/Redis MVP. Include healthchecks, env file usage and convenient make targets." | Added Dockerfile, Compose services and Makefile targets. | Checked required files and target names. |
| OWASP review | OpenAI Codex / GPT-5 | "Review this AI-assisted telecom billing MVP against OWASP Top 10. Produce a table with category, location, issue, impact, criticality and mitigation." | Produced `SECURITY_ANALYSIS.md` and report table. | Compared risks against implemented controls. |
| Report | OpenAI Codex / GPT-5 | "Write main.tex in Russian for Assignment 2. Include topic, goal, variant, AI tools and concrete prompts, OWASP Top 10, retesting and a comparison table with at least 10 criteria." | Produced `main.tex`. | Checked required sections and LaTeX structure. |
| Cross-check | Gemini | "Check whether the Assignment 2 report contains all required sections: AI tools, concrete prompts, OWASP analysis, retesting and comparison with Assignment 1 by at least 10 criteria." | Used as secondary review guidance for report completeness. | Final responsibility kept with human/Codex review. |

The AI outputs were not accepted blindly. The final implementation was checked against the assignment text, security requirements and smoke checks.
